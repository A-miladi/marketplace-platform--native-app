export const GALLERY_MULTIPLE_IMAGE_OPTIONS = {
  compressImageQuality: 1,
  includeBase64: true,
  multiple: true,
  mediaType: 'photo',
};

export const GALLERY_SINGLE_IMAGE_OPTIONS = {
  compressImageQuality: 1,
  includeBase64: true,
  mediaType: 'photo',
};
export const CAMERA_OPTIONS = {
  width: 400,
  height: 400,
  // cropping: true,//if we are cropping the images we have a bad quality in Android
  compressImageQuality: 1,
};
