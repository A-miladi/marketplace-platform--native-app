import {RW} from '@/utils/DimensionsChange';
import {StyleSheet} from 'react-native';

export const RelatedStyle = StyleSheet.create({
  container: {
    width: RW(100),
    flex: 1,
    paddingHorizontal: 25,
    backgroundColor: '#Fff',
    alignItems: 'center',
    paddingVertical: 8,
  },
});
