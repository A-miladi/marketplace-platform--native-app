import {StyleSheet} from 'react-native';

export const QuestionStyle = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    paddingBottom: 12,
    gap: 12,
    position: 'relative',
    marginVertical: 12,
    paddingHorizontal: 20,
  },
});
