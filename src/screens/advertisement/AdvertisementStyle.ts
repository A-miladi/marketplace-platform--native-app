import {StyleSheet} from 'react-native';

export const AdvertisementStyle = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    fontWeight: 'bold',
    gap: 4,
    backgroundColor: '#fff',
  },
});
