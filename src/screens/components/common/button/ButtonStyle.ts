import {StyleSheet} from 'react-native';

export const ButtonStyle = StyleSheet.create({
  regular: {
    fontSize: 14,
    fontWeight: 400,
  },
  bold: {
    fontSize: 14,
    fontWeight: 500,
  },
});
